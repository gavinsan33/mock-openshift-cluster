apiVersion: v1
kind: Service
metadata:
  name: mock-dcgm-exporter
  namespace: monitoring
spec:
  selector:
    app: mock-dcgm-exporter
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 8080
